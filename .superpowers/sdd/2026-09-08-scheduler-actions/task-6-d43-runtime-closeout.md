# D43 实际导入验证与测试环境清理

2026-09-14。代码版本 `07568799dac65ee3558daccbab4fef58dd8f79ee` 在隔离环境通过专项验证，尚未将 D43 发布到生产。

导入 worker 连续空闲超过 35 秒后，经原有 Next.js 代理上传一份新的 188 字节 PNG。只提交一次，记录到一次队列唤醒；任务从发布到领取耗时 **0.511250 秒**，从创建到核心导入完成耗时 **2.707134 秒**。此前 R6b 同类样本的发布到领取耗时为 25.048846 秒。该结果是一组实际样本，不是 P95。

验证保留了两个父子任务、10 条任务事件、下载与导入记录，以及完成检查点 v2。原文件内容、关联作品、四个存储文件记录、派生处理记录、精确的配额变化，以及 53 张表和受管理目录的变化均通过核对；原有数据和运行参数保持不变。领取后的健康接口读取使用了仍在有效期内的正常资源预算快照，测试环境原有的 scheduled worker 缺失提示单独保留。

代码回归为 188 项通过、零跳过，相关 Ruff 检查通过。下载、operations 和 maintenance 的唤醒路径已有代码与回归覆盖，其实际延迟尚未单独测量。后台媒体、搜索与 Git 的处理状态独立记录。

较早一次预检因主机内存压力在上传前停止，未修改业务数据。另一次已完成上传测得 0.704376 秒领取、5.673953 秒核心完成，但事件在完整采集前被正常自动归档，缺少领取后的健康读取，因此只保留为有限的实际测量记录。两次记录均保留，已完成的上传没有重跑。

证据目录为 `/volume2/docker/auto-gallery-button-audit-artifacts/20260908`。原始表、凭据相关快照和导出保持私有，不纳入 Git。

| 证据 | SHA-256 |
| --- | --- |
| R3 的 26 个输入清单 `reconcile-inputs.json` | `b3d68e77995e395eb528f6bb8d7f6212ccd769b6b75ee2a063f20cff50eae5b2` |
| R3 实际核对结果 `d43-actual-reconciliation-final.json` | `de88b971b74a8b09effd6e6127e6cd3dc1cc03c381213313c2b0112138909a8f` |
| 独立结果复核 `task-6-d43-r3-actual-closeout-review.md` | `050d7a1a2f6767d9b8c9cf888a9c64144023bfdf8f169b31be36eb849e8f9725` |
| 专项关闭记录 `d43-import-wake-scoped-closeout-20260914.json` | `0b1db8241f2ab3febb934178c503f32647839616465630467d442e47b5d1e5dc` |
| 操作覆盖清单 `action-coverage-progress-20260914-r7.json` | `add8eaa955e55b3862b61ea9d8b9dd825d4165f167d5b562d2e20b57ff6a3441` |

完成回归的运行器、辅助 PostgreSQL 和测试 Redis 已停止并删除。删除前完成了 PostgreSQL 全量 SQL 导出和 Redis RDB 导出，保留配置、日志、卷、镜像和历史证据。累计删除 59 个已经结束使用的测试容器，当前 7 个容器仍用于全站验收。清理后复查，生产的 10 个服务健康，容器身份没有变化。清理结果 SHA-256 为 `54f5fba653a632f4d471a2ffd454ed6b6228540610284d1ae9109df423078913`；生产复查记录为 `aef7f8e4c18e4548fb3c6fa1ba6afc9bbfd7412710a8ecb8da89f89f6decb08a`。

全站主操作证据仍为 **39/155**，完整变体验收未完成。D40 的旧任务兼容验证和其余模块交互验收继续执行；正式 master PR 的验收门槛尚未满足，Cordis 重构继续后置。

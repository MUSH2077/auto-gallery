# 调度同步修复与全站按钮语义验收

User-approved implementation plan, 2026-09-08. Base: b3340c26470b0dd47193a27761c5b5dd7c0061ec.

## Global Constraints

- First deliver the scheduler hotfix after its real acceptance; then complete every frontend action audit and repair. Cordis and other architectural rewrites remain deferred.
- A sync-all batch automatically resumes after queue capacity returns and finishes only after downloads and their main imports reach terminal outcomes. Derived media, search and Git projections remain separate background work.
- Preserve gallery-dl, the existing resource limits/governance, private account ownership, scheduling configuration, production data, and the immutable latency-first source/evidence. Only work in the scheduler-actions checkout. No production mutations by subagents.
- New batch admission returns HTTP 202 and a durable task identifier. No source iteration, network work, long waiting or file traversal in the request handler. Target submission P95 <= 1 second through the real Next.js proxy on the 809-source acceptance fixture.
- Batch slices process at most 25 sources or 20 seconds, deferring transient admission failures 30 seconds to 5 minutes without occupying a worker while waiting. Persist a unique batch/source item and bind its download dispatch in the same transaction.
- Preserve all three sync modes. manual_all_enabled includes manual subscriptions but still checks enabled status, ownership, credentials and provider download capability. force_eligible and due_scan retain their narrower eligibility.
- Final results distinguish queued, waiting, downloading, importing, succeeded, skipped and failed. Partial failures never produce an all-success notification. Duplicate submission and restart recovery must not duplicate downloads. Cancellation stops pending publication and safely cancels owned unfinished children, preserving completed results.
- Global sync execution, viewing and control require admin/system privileges; private actions keep their membership boundaries. Repair the known legacy batch by reconciling receipts/current jobs before scheduling its still-unprocessed sources.
- Audit all 52 route files and shared controls, including modal/menu/mobile actions. Validate real state changes, not only a fulfilled HTTP mock or a toast. Chinese/English, desktop/mobile, admin/restricted, empty/pending/error states are in scope.
- Keep layout/functionality; change only necessary copy, state, loading and action behavior. Destructive actions are tested only on isolated fixtures. No missing actions, unexplained skips or unresolved functional defects may be marked passed.
- Retain the old production observation and annotate the known fault in a new artifact; start a new version-specific observation only after a successful reviewed rollout.

## Confirmed evidence

September 15, 05:54 UTC continuation: subscription business run C03 completed the Chinese desktop workflow and the mobile subscription join, then failed because a late search response replaced newer input. Commit `72543ae` completes the shared search correction: locally edited text survives old URL acknowledgements and old compose responses, including editing that begins before the first keystroke. All 18 focused browser regressions passed against its built image. Separate real Next.js/backend tests passed in all four language/device contexts for rapid filtering, typing, browser back/forward and direct links. Database/file comparison found 52 tables identical, only the two users' login timestamps changed, and asset files unchanged with a verified log append.

C03 remains recorded as failed. Its completed desktop actions and mobile join are retained for continuation without replaying writes. The two existing search checkpoints were finalized through the public bounded delivery function, processing zero documents; the membership and canonical indices are ready at 231 and 175 documents. The completed build container and superseded frontend container have been removed with logs retained. Seven containers remain for the unfinished business acceptance; full-site coverage remains 42/154 and the master PR is pending.

September 15 acceptance update: repository detail navigation now passes the same real Next.js/backend read-only test in Chinese and English, on desktop and mobile. An invalid isolated history fixture was corrected in one receipt, preserving its failure reason. The subsequent restricted-user test exposed three forbidden requests: Git history lacked the `curation` permission gate, and Jobs fetched the global workbench without `system` permission. Frontend commit `59929d8` adds those gates. Its complete image build and mandatory checks passed; the unchanged browser test then passed all four contexts with ten navigation checks each, no response/page errors, and no business writes. Independent database/file reconciliation found only expected login timestamps and a verified log append. The superseded acceptance frontend and this build's container were removed after preserving logs.

These results close the two permission-query defects, not the whole-site audit. Matrix `action-coverage-progress-20260915-r17.json` retains 42/154 mounted families with previously counted scoped evidence; repository navigation adds partial evidence because graph filters, history pagination and remaining edge cases still require validation. Subscription acceptance will resume after the already completed source deletions, preserving the original failed runs and avoiding duplicate syncs. The seven active acceptance containers remain necessary. The formal master PR remains pending Task 6 acceptance.

September 20 creator-list update: A001–A008 now pass their declared primary workflows through the frozen R11 Next proxy and real backend in Chinese and English at desktop and touch-mobile sizes. The run recorded 47 allow-listed writes, 12 temporary creators, eight terminal hierarchy-deletion tasks, zero page errors, and complete UI cleanup. It covers duplicate-create suppression in the same event loop, the corrected search/navigation debounce, selection, favorite, archive/restore, permanent and batch deletion, actual permission failures, the virtual name rail, and genuine error Retry. The isolated Danbooru boundary matched twelve one-call rules and returned twelve 404 responses with no rejected request or external upstream.

The post-run comparator passed all 53 tables and both managed roots. Pre-existing rows and payload/library files are unchanged; exact retained lineage is limited to 36 curation commits/changes, 36 repository-less Gitllery intents, 12 terminal search projections, 30 terminal delivery receipts, eight deletion tasks with 32 events, the creator-index watermark, and three test-principal login timestamps. The twelve creators and twenty-four local-name aliases are absent. Three 39-byte search lock tokens changed while processing those projections, with path/type/size/mode preserved. Matrix `action-coverage-progress-20260920-r20.json` (SHA-256 `7e98b6e33e1e3844950d6318a016326023392596173e07c1e68f484d4e1b723e`) advances scoped evidence from 54/154 to 62/154. Mixed batch failures, active-task blocking, delete-files, linked-provider creation, broader failures/permissions, and concurrency remain pending; whole-site acceptance and the master PR gate remain open.

September 20 creator-detail update: A009–A018 now pass the real Next/API/PostgreSQL/Redis primary workflows in Chinese and English at desktop and touch-mobile sizes. The run covers profile persistence, link creation/verification, repository enable/disable and task-backed sync, activity year failure/retry, clickable tag filtering and clearing, paging/open-all, slideshow controls, reference retry, alias fill and external navigation. The repository action now calls its own sync endpoint; link creation includes the API-required `creator_id`; write controls use the backend-aligned curation/subscriptions permissions. Focused browser checks passed 23/23, final backend deletion/media checks passed 27/27, migration-chain checks passed 5/5, and typecheck/API/routes/i18n/lint passed.

The first cleanup exposed two persisted bookkeeping defects and both are fixed with failing-then-passing integration coverage. Permanent deletion now returns manual-upload quota in the same transaction as the `purged` state, using immutable uploader identity for new uploads and a legacy username fallback; a failure after purge followed by retry returns the bytes exactly once. Purge also changes unfinished derivative requests to terminal `cancelled` before deleting their source assets. Migration `fd35e7f9ab13` reconciles existing quota from non-purged manual metadata plus unimported artifact rows, adds the cancelled derivative state, and repairs historical derivative rows for already-purged assets.

The final fail-closed comparator (`creator-detail-r1-preservation-20260920.json`, SHA-256 `987dae926e316b0acf87c35ccb3fdf6b63d94ade48fce77eaf0c5b0140c59005`) passed 53 tables and both managed roots. Creator/source/link/subscription/repository/private-membership rows are absent; 13 works and assets remain only as purged audit tombstones; all fixture media bytes are absent; no pre-existing path or database row was removed. Upload quota is 752 bytes before and after, 13 derivative requests are terminal cancelled, search projection has zero pending rows, and the prior twelve supplier rules were restored exactly. Matrix `action-coverage-progress-20260920-r21.json` (SHA-256 `c683941282438e6f9339ff9c575b0f448ef42ca3c749d6fad7aff2922f995600`) advances scoped evidence from 62/154 to 72/154. Broader per-action variants remain listed; whole-site acceptance and the formal master PR gate remain open.

September 20 duplicate-review update: A019–A020 now pass the real Next/API/PostgreSQL workflows in Chinese and English at desktop and touch-mobile sizes. Both selection-bar Cancel and confirmation-dialog Cancel were exercised in all four contexts and emitted zero merge requests; the final confirmation emitted exactly four. Candidate ordering is now deterministic by creation time and ID, so the visible retained target does not drift across refreshes. Successful merges move and deduplicate aliases, links and source identities, merge favorite/description state, transfer a sole source subscription, and rebuild surviving private membership projections. A target/source pair that both own subscriptions is rejected before any mutation.

The Chinese desktop run also exercised a genuine partial result: one source merged and one dual-subscription source returned `merge_rejected`; the page retained only the failed source and displayed reviewable partial-failure feedback. Backend creator-dedup and security regressions passed 19/19, the focused browser suite passed 8/8, lint passed, and typecheck/API/routes/i18n passed in the fixed-dependency container. The fail-closed comparator (`creator-duplicates-r1-preservation-20260920.json`, SHA-256 `5c438ea3ea0a0b4801312126e8e7d52c839f78592a82d569d1c72a6073b9e7c9`) restored all 53 tables and both managed roots exactly outside ephemeral lock contents; no fixture row, search document or media path remains. Matrix `action-coverage-progress-20260920-r22.json` (SHA-256 `5af7e80e5a790dc833533ed0e77257042ebca745e6257f4ec5f5d24bea45792b`) advances scoped evidence from 72/154 to 74/154. Concurrent merge races, transport/interruption variants, the source-identity candidate strategy and larger groups remain listed; whole-site acceptance and the formal master PR gate remain open.

September 20 curation-ledger update: A021–A024 now pass the real Next/API/PostgreSQL/Redis workflows in Chinese and English at desktop and touch-mobile sizes. URL filters and baseline visibility are linkable, expanded commits no longer retain a stale “more changes” count, and both destructive actions use reviewable confirmation. All four revert cancels and purge cancels produced zero writes. Confirmed reverts each returned a genuine partial result with five restored works and one preserved conflict; confirmed purges each submitted exactly the one frozen preview work, changed its persisted state and removed its fixture file. A focused restricted-user case proves curation members retain ledger/revert/backfill controls while the administrator-only purge preview and button remain absent.

Backfill now returns the durable administrator-operation contract and restores its exact task after page reload through `/curation/backfill/latest`. The first real run exposed an additional delay: one already-durable work group ended each slice, so 68 no-op groups paid the 30-second recovery cadence individually. The repaired iterator scans durable groups within the same bounded time slice; the existing task completed on attempt 15 by skipping the remaining groups in one 0.670047-second worker execution, and the next three language/device tasks completed in two attempts each. Backend API tests passed 14/14, focused continuation tests passed 3/3 against disposable PostgreSQL, frontend type/API/routes/i18n checks passed, and the focused browser cases passed. The fail-closed comparator (`curation-actions-r1-20260920/preservation.json`, SHA-256 `f388f1369bbaab9df35188bf9da419a906b10a624b24b7d487908961e01a61f5`) matched canonical rows across all 53 tables and both managed roots outside ephemeral lock contents. Matrix `action-coverage-progress-20260920-r23.json` (SHA-256 `892b3a738f08c7ac120623d77c073a297713177f7af3eae2c9599d88b25b67f0`) advances scoped evidence from 74/154 to 78/154. Capacity denial, Redis handoff interruption, active cancellation, revert concurrency and broader purge variants remain listed; whole-site acceptance and the formal master PR gate remain open.

September 20 asset-dedup update: A025–A028 now pass the real Next proxy/API/PostgreSQL/Redis/worker/filesystem primary workflows in Chinese and English at desktop and touch-mobile sizes. Four scan submissions returned 202 in 224–328 ms, survived page reload with the exact task link, and completed through three bounded worker attempts each. Scan completion created and hardlinked one exact cross-source pair. Four merge cancels and four separate cancels emitted no decisions; confirmed actions produced exactly four merged, four separate and four deferred cases at revision 2. The four manual visual merges completed their storage outboxes, moved the non-representative files into the retention area and scheduled future purge events while preserving all 26 Works and source rows. One cold-development console 404 did not map to a backend request and did not reproduce on a warm authenticated page; it remains recorded in the raw diagnostic rather than being silently discarded.

Asset scanning now uses the shared durable-operation controller and a typed 202 response. `/admin/dedup/scans/latest` also exposes retryable terminal tasks, so a failed scan remains addressable after refresh and Retry advances the same TaskRun instead of creating another scan. Separate now requires confirmation; merge, separate and defer display their exact completed action and invalidate the complete dedup query family. Final backend reconciliation/coordinator tests passed 17/17, focused browser tests passed 4/4, and typecheck/API/routes/i18n/Ruff checks passed. The isolated databases were exported before removal; Redis DB 14/15, the test API/workers, fixture roots and group frontend container were removed, while seven shared acceptance and ten production containers were retained. Matrix `action-coverage-progress-20260920-r24.json` (SHA-256 `e0554c4d54c641f4f8a28f2c5aa4e0f44134bca2629f98ca0e6544b6c30da244`) advances scoped evidence from 78/154 to 82/154. Zero-input, capacity denial, active cancellation, stale-review concurrency and transport-failure variants remain listed; whole-site acceptance and the formal master PR gate remain open.

At 2026-09-08 03:14:37.477807 UTC the real parent task 3330dcea-e850-422f-9de8-3e5fd62fe6ef started manual_all_enabled over 809 sources. It finished at 03:16:05.215947 UTC (87.73814 seconds), enqueued 102 and skipped 707 for queue_saturated, with no recorded enqueue errors. The Next.js proxy logged ECONNRESET at 03:15:07.467204618 UTC. The installed proxy source defaults to 30000 ms. The source request loops synchronously; its mocked tests bypass real enqueue behavior. Tags include_all also logged ECONNRESET at 03:11:42 UTC. UI task retry rules and global task permissions differ between surfaces. API client spreads options after merged headers, overwriting auth with custom headers.

## Task 1: Durable scheduler batch backend

Implement the entire backend scheduler hotfix, leaving frontend and generated API exports to Task 2. Read backend/app/api/admin/scheduler.py, services/operations.py, jobs/admin_operations.py, services/subscription_enqueue.py, services/download_dispatch.py, services/tasks.py, services/operation_attention.py and api/tasks.py.

Add an additive migration after f9e1a3b5c7d9 and models for durable batch identity/idempotency and unique per-source items (one or two tables as needed for real database uniqueness, not transient Redis authority). Persist mode, actor, snapshot/initialization state, source identity, next retry, attempts, linked download id and final outcome. Do not cascade-delete a pending/result item merely because its source or operational DownloadJob disappears. User-facing parent is a registered subscription-sync-batch administrator TaskRun, on the existing operations queue with system permission. Reuse attempt fences, deterministic dispatch, recovery and handoff from operations.py; do not invent a second scheduler runtime.

POST scheduler/sync-now keeps mode input and gains optional request_id UUID; returns 202 with task_id, job_id, status, operation_type, mode (status enqueued for new acceptance). Same request identity locates the existing task; mismatched mode for same identity is a structured conflict. An active global batch is a structured 409 with task_id so the client can open it. Missing request_id remains supported. GET existing task/operation APIs expose progress and results; provide a bounded paginated batch-item endpoint if needed to inspect source reasons. Use system permission consistently for global execution/status/control, without widening access to private child credentials. Request transactions only persist batch and initial dispatch; iteration occurs in the worker. Snapshot eligible sources durably when processing begins, then recheck mutable eligibility before enqueue.

Process no more than 25 sources/20 seconds per slice; fair keyset/next-retry selection ensures one blocked source cannot starve others. Queue/Redis pressure, lock busy and temporary infrastructure failure are waiting, with 30..300 second capped backoff. Invalid/disabled/auth-ineligible/unsupported sources are explicit skips; unexpected nontransient exceptions record failure per item and let other sources progress. Avoid source-level full rollback expiring caller ORM state: process stable scalar IDs and isolated/fenced transactions. Modify enqueue helper with a narrow optional batch binding/mode parameter so manual_all_enabled genuinely includes manual member subscriptions, while private/manual and automatic existing behavior stays correct. Batch-item download identity MUST be committed atomically before Redis publication. Resolve existing dispatch/receipt on crash/retry, including a very fast worker whose rows have already compacted.

Track download+import completion using actual current domain state and durable RepositorySyncReceipt, respecting recoverable StorageArtifact states. A completed enqueue loop is not batch completion. Waiting for children uses durable delayed slices, not sleeping workers. Persist parent progress counters; final aggregate complete only when all items terminal, failed with partial result if any failed; skipped-only/no-op is explicit, never all-synced success. Cancellation is supported through existing task API: stop further item publication with attempt fencing and safely cancel only this batch's unfinished child jobs, never unrelated running jobs. Preserve completed rows/receipts.

Provide a dry-run/apply recovery command for legacy batch 3330dcea-e850-422f-9de8-3e5fd62fe6ef; default dry-run, no production invocation by implementer. Preserve the old batch and derive a new linked batch from its recorded candidates/results. Reconcile its original 102 enqueued identities and the 707 queue-skipped source identities against subsequent outcomes/inflight jobs. Only genuinely unprocessed eligible items may enqueue again; repeated recovery is idempotent.

Use TDD and real isolated PostgreSQL for persistence/constraints. Cover 202 bounded admission, system/tasks-only/anonymous permissions, duplicate request/active batch, all three modes incl manual membership, zero candidates, saturation/resumption, fair transient retries, unexpected failure, cancel races, child import completion, restart before/after publication, receipt after compaction and legacy recovery. Existing tests which monkeypatch the complete enqueue path are insufficient alone. Do not run destructive pytest against production. Root provides the isolated ag-button-runner and dedicated DB; ask root for execution coordination if unavailable. Add tests and report honest exact commands, outcomes, and any remaining integration limitations. Commit only your scoped files. Do not spawn subagents.

## Task 2: Scheduler frontend and request contracts

Consume Task 1's actual accepted/task/item contracts. Read admin-web/AGENTS.md and the locked Next.js guide before code edits. Fix client header merge so auth plus caller headers coexist; distinguish network/HTTP/business rejections while preserving structured errors and 204 behavior. Scheduler submits a stable per-intent request_id, tracks its task across refresh/navigation, opens existing task on 409, and renders durable progress/terminal outcome with correct Chinese/English labels. Both batch buttons require system/admin and preserve three backend modes. Toast accepted != complete; final download/import failures or skips cannot display all-success. Integrate existing notifications/task detail and polling; keep a periodic fallback when websocket is unavailable. Prevent double submission without leaving controls stuck after failure. Update all triggerSyncNow callers, OpenAPI export and generated TypeScript types. Add browser and backend-contract tests which exercise responses/refresh/error scenarios, not only request payload. No production/browser write tests. Commit scoped changes and report.

## Task 3: Real scheduler acceptance and hotfix rollout

Build/test exact backend+frontend candidate in a labelled isolated environment with genuine Next proxy, PostgreSQL, Redis, workers and local provider fixtures. Run 809-source bounded queue test, retain raw request P95 samples and actual eventual outcomes, include a >30 second processing run. Exercise reboot/cancel/Redis capacity/dispatch crash/compaction paths; verify files and receipts, final zero remaining eligible items, no duplicates. Run affected backend checks, frontend build/typecheck/contracts/i18n/routes, then independent broad review. Package and deploy through the repository's existing reviewed release workflow, with source/image identity, backups, migration and health checks; user has explicitly authorized this staged release. Root owns deployment, never subagents. Review/apply legacy recovery only after dry-run reconciliation is concrete. Keep old observer and new-version observation separate; do not claim a 24h pass from launching a process.

## Task 4: Server action capabilities and bounded tags

Introduce one backend task action policy used by action endpoints and serialized capabilities (available_actions and disabled reasons), preserving authoritative permissions and state checks for downloads/imports/admin. Cover retry vs repeat-completed-sync, pause/resume/cancel/delete and partial batch outcomes. No private visibility expansion. Replace all-tags first-screen dependency on an unbounded read with bounded loading while keeping every tag accessible and existing visualization usable; avoid repeatedly expensive per-tag count queries. Implement the smallest compatible bounded read/summary contract required and appropriate large-data query tests. No frontend implementation in this task; contract consumers are Task 5. Use meaningful tests and independent review.

## Task 5: All frontend action semantics and defects

Use the root's full route/control inventory and Task 4 capabilities. Every visible control family including shared menu/modal/mobile/navigation/search/selection must have an audit entry: copy, scope/object, permission, request, state effect, feedback, invalidation, tests/evidence. Fix mismatches across works/tags/creators/subscriptions/discovery/upload/jobs/scheduler/notifications/data/settings. Define all-visible vs selected vs all-filtered scope; cancel confirmation must not submit or clear selection. Shared capabilities power task list/drawer/batch tools; error preserves input and selection, success invalidates all affected views. Correct partial-success/business-decline handling instead of treating all resolved requests as success. Bounded tag loading preserves access to all tags and reports loaded/total without pretending truncated rows are complete. Keep current layout, bilingual copy, focus/keyboard access and mobile parity. Add meaningful Playwright coverage for every inventory action family and semantic state; no real provider/production destructive calls, no tests claiming coverage merely because an AST button exists. Commit reviewed changes.

## Task 6: Whole-site acceptance, release and handoff

Run all inventoried control families in Chinese/English desktop/mobile, admin/restricted, empty/pending/error fixture states through the actual application. Application-side writes go through real API/DB/files/queues; external providers alone may be fixture adapters. Validate resulting persisted state and task outcomes, not just toasts. Retain raw failures/screenshots/traces outside repo, exact covered/uncovered action matrix and remediation links. Missing or unexplained skipped actions and unresolved functional defects fail acceptance. Run required regressions/typechecks/API/routes/i18n and independent whole-branch review, publish final reviewed version through existing workflow, verify production and start version-specific observation without rewriting earlier evidence. Deliver code, inventory and test evidence; report outstanding elapsed-time gates honestly. Do not delete worktree/evidence while it supplies production provenance or unresolved observation.


## Task 7: September 13 preview recovery and search settings comparison

User-authorized incident follow-up precedes completion of Task 6. Production has 926 never-claimed media derivatives, including 659 thumbnail requests, and 278 import projections. Search receipt 88fd2f99-b50f-4bdf-b3b7-8bb47c547901 is ambiguous for subscriptions/settings; operator-verified Meili task 22328 succeeded. Root owns production receipt reconciliation, task-history cleanup, test-container removal, immutable rollout and final acceptance.

Implementer scope is initially search settings comparison and focused tests only: backend/app/services/search_delivery.py and new or existing focused backend tests. Fix _contains_settings with path-aware unordered comparison ONLY for filterableAttributes, sortableAttributes, nonSeparatorTokens and typoTolerance.disableOnAttributes. Require string list types for these fields; reject wrong types/missing values and preserve ordered comparison for searchableAttributes, rankingRules and all unknown arrays. Do not mutate input lists. Test reordered equivalent settings skip the PATCH through the real delivery logic; membership changes and ordered changes still require update. Use RED/GREEN evidence and existing isolated ag-button-runner/PostgreSQL/Redis only. Root will provide exclusive fixture-test ownership; do not use production. No generated API edits or unrelated uncommitted D39/D40 edits. Commit only owned files, write report with commands/counts/log paths and self-review. Do not spawn subagents.

Root additionally adds truthful preview blocking feedback and validates original backlog drain after verified receipt reconciliation. Reuse durable receipt and owner-scoped marker recovery, preserving resource protections. No guessed remote identity or blind lock deletion.

## Task 8: Authorized historical cleanup and ephemeral test environments

User confirms download d7b6d329-c9c4-496b-899e-aa3e96a3fc22 was intentionally deleted. Its orphan TaskRun ba795521-1426-4145-030d-ee0d527f2a67 is removable historical projection after no-live-work verification, not a reason to re-download. Preserve the failed repository receipt and record the user-confirmed deletion reason. Existing development task_history already deletes operational wrappers atomically; deployed old delete_download does not. Verify current delete callers and regressions.

Export and resolve old discovery ed9176d5-cd39-4bf6-a819-eca2c3c901db after its later successful scan, and batch task 4cddb7b3-adf0-4267-a1bf-74d5be6425a1 after both late child receipts complete. Preserve original 807/809 failure audit and durable batch items, recording later recovery; never redownload 809 sources.

Snapshot exact test container IDs, projects, mounts, networks and restore evidence. User authorizes deleting completed test/dev containers; retire old environments first and delete remaining current-round test containers after validation and exports. Never remove production auto-gallery project containers, real data, unrelated applications or historical emergency recovery evidence. Preserve evidence directories and volumes; no global prune. Test database tmpfs must be exported before stopping. Later acceptance environments are recreated on demand and removed after use. Formal PR to master only after Task 6 passes; current branch includes PR 63 history, document overlap without auto-merging or closing existing PRs. Cordis remains deferred.

September 13 execution update: the deployed search hotfix has drained the original 926 derivative rows and 278 import projections. All 659 requested preview files passed complete file decoding and source-fingerprint verification. Historical task cleanup retained original failed receipts and batch results. Completed test-container retirements total 54, with evidence and volumes retained; the active acceptance environment remains until its checks finish. Whole-site acceptance remains 36/155 mounted action families, so the formal master PR gate is still open.

September 14 execution update: the D43 immediate-import wake correction at source `0756879` passed 188 regressions and one complete actual isolated latency proof: 0.511250 seconds from publication to claim, 2.707134 seconds for the core import, with both TaskRuns, ten TaskEvents, checkpoint v2, exact file content and 53-table preservation. This is one sample, not P95, and D43 has not been deployed to production. Completed test-container retirements now total 59 after preserving PostgreSQL/Redis exports; seven current acceptance containers remain. Production's ten services stayed healthy. Primary workflow evidence is 39/155; all-site acceptance and the formal master PR remain pending.

September 14 later execution update: `22fa7fd` fixes creator selection beyond the first 50 entries in subscription creation and both Danbooru selectors, resolves subscription detail creators by exact ID, and localizes network errors. The exact frontend image passed all build checks. Four focused regressions passed after test-only corrections in `16c7ab1`; their first failed run is retained, not counted as a pass. Actual EN/ZH desktop and touch-mobile tests verified localized transport failures, Retry against the real registry, and Refresh disabled while its real request is pending. All 53 tables and 487 file entries were checked: only the single admin login timestamps and a verified backend-log append changed. `A151-I18N-02` is closed; these supplemental checks do not add primary action families.

The latest immutable coverage matrix is `action-coverage-progress-20260914-r14.json` (SHA-256 `20004abf7228b534980906f59bdf075989505653daa28f0a39aec767642917e0`): 42/154 mounted action families have scoped actual evidence, with six unmounted families separately accounted for. Whole-site acceptance remains false. Subscription business acceptance is still pending: its first actual run exposed the creator-selector defect before any business write; source review also found that private subscription labels are absent from the shared search document. The isolated subscription index inventory separately records 175 database rows, 13 indexed rows, and 162 missing documents. Index preparation and a compatibility-preserving private-label search fix must not be conflated or reported complete prematurely.

Completed test-container retirements total 80 after the old frontend and this build's BuildKit container were removed with logs retained; the seven active acceptance containers remain for unfinished checks. Production's ten services were healthy with no restarts or OOM at 2026-09-14 10:55 UTC, approximately 22.13 hours into the separate 48-hour observation. `origin/master` remains an ancestor of this branch; PR 63 remains open and unchanged. The formal master PR and Cordis rewrite remain behind the existing acceptance gates.

September 14 membership-search update: the isolated canonical index was repaired through native bounded delivery: 162 missing documents, four complete receipts, checkpoint 89/89 and 175/175 documents. Independent preservation review passed; no business rows or asset files changed. With that index ready, twelve real proxy queries demonstrated four canonical positive controls and eight private-name misses, separating the application defect from index lag. The repair is fixture readiness, not a product acceptance claim.

The private membership index implementation now has independent source/test reviews and a combined real PostgreSQL/Meilisearch regression run of 233 passed, zero failed or skipped (log SHA-256 `4d05f16742e47a5bbb60d4d52ab25b01d86784b389b705e64d04e274112d8967`). Coverage includes multilingual/private search, actor-filtered counts and pages, rename/delete/rejoin, native RESTRICT rollback, transactional fanout and persistent bootstrap slices. Actual separate-process bootstrap recovery and the original subscription browser workflow remain pending candidate deployment; primary full-site coverage remains 42/154. No production deployment or formal master PR is claimed by this source milestone.

September 14, 12:48 UTC verification update: candidate `f7fb537` now runs in the isolated backend. Native membership bootstrap completed 230 documents through 32 distinct Python processes continuing one durable build, with 14 completed receipts. This proves process-to-process continuation; an RQ supervisor restart remains unproven. All existing rows across 53 tables were preserved; only the new build, index state and receipts were added. Of 487 filesystem entries, only four native lock stamps changed, and all four locks were verified released.

The original twelve queries now pass through the real Next.js proxy, including all eight previously failing private-label queries (result SHA-256 `fe9aff5fca668aa11c36c6ca756d483549772b2ec889d94436866856d29e208b`). Query verification changed only two users' login timestamps and appended 7,309 bytes to the backend log; business records and other files were preserved. Subscription button workflows still require their separate browser and persistence acceptance, so coverage remains 42/154 and full-site acceptance remains false.

The replaced, stopped test backend was removed after replacement health and query verification, bringing completed test/development container retirements to 87. Seven containers remain for unfinished acceptance; their data and retained recovery evidence have not been deleted. Production's ten services are healthy with no OOM or restarts at 12:48 UTC, 24.00 hours into the required 48-hour observation. The formal master PR remains pending full-site acceptance.

September 20 data-center update: action families A029-A035 now have scoped actual evidence through the real Next.js proxy, API, PostgreSQL, Redis, RQ worker and isolated filesystem. Disk import retained `reset_ledger=true` and completed one real child import; metadata cleanup removed one evidence-qualified gallery-dl sidecar while preserving untracked JSON; rebuild wrote metadata; the no-pending creator re-enrichment completed; backup produced a restorable 49,599-byte archive; and two confirmed clear-all operations returned domain rows and files to zero. Wrong confirmation remained disabled and cancel sent no request. The four language/viewport layouts rendered all seven controllers without overflow or console errors, while an actual tasks-only account received API 403 and no action controls.

The real integrity run exposed three stale schema/path assumptions and a candidate-image import-root error. The final retry reused TaskRun `099f21ee-4019-4d2b-ab3f-4478d6e6c392` at attempt 2, found the seeded missing thumbnail through `asset_sources`/`work_sources`, and did not report its existing relative media path as dead. `backend/Dockerfile.candidate` now resets inherited `PYTHONPATH=/app`, preventing RQ from loading the replaced image's old `/candidate-app` tree. The combined backend regression passed 30/30 in 224.24 seconds, focused Playwright passed 4/4 in 17.2 seconds, Ruff and `git diff --check` passed, and exact backend/frontend images are recorded in the retained report.

The immutable progress ledger is `action-coverage-progress-20260920-r25.json` (SHA-256 `b61391bc21154b5016c75b18d8b05b585580ebe31f20b43de43875baca836709`): 89/154 active action families have scoped actual evidence; six unmounted families remain separately recorded and whole-site acceptance remains false. The data-center acceptance summary is SHA-256 `f4484cb7e1e1adc082ecedef561dbe5bc31f3311593362f94b49a8ec696a99ee`. After exporting both disposable databases and all logs, the three group containers, dedicated network, databases, Redis DB 12/15, temporary runtime tree and local Next development server were removed. The seven shared acceptance containers were retained, and all ten production containers remained healthy. The formal master PR and Cordis rewrite remain gated on the remaining action families.

September 20 scheduler-ledger update: A059 now has exact-current image evidence in Chinese/English on desktop/mobile for attention pagination, plan expansion, server-side search, due filtering and plan pagination over the retained 77-row decision set. Every request was bounded to 25 rows, all four contexts had no console errors or horizontal overflow, and the recorder observed no business writes. The retained A060/A061 evidence was requalified against current source ancestry: due-scan received a durable 202, a duplicate resolved to the same active task through structured 409, and cancellation persisted its cleanup reference; four language/device manual-all-enabled submissions persisted unique request IDs and cancelled safely; the 77-minute 809-source batch finished with 809 durable succeeded items and zero waiting, skipped or failed items.

Current scheduler regressions passed 13/13 frontend and 90/90 backend tests. The test harness now writes screenshots through Playwright's per-test output path and recognizes expected fixture WebSocket failures at the active origin instead of requiring port 13000. The isolated regression database was exported then dropped, Redis DB 15 returned to zero, the temporary frontend and local development server were removed, and the shared acceptance environment remained intact. The new immutable ledger is `action-coverage-progress-20260920-r26.json` (SHA-256 `452eaf72c13ebb421ab4a4a7f2dce877582653f896f49b1146037ea047c9ea8a`): 92/154 active action families now have scoped actual evidence. Whole-site acceptance and the formal master PR remain pending.

September 20 settings-diagnostics update: A084-A091 now pass their declared primary workflows through the real Next.js proxy, current API, PostgreSQL, Redis and maintenance worker in Chinese/English on desktop and touch-mobile. Proxy and gallery-dl edits covered navigation cancel, aborted requests that retained input, held double clicks that emitted one write, persisted readback and exact baseline restoration. Four proxy diagnostics completed with nine result targets each, and four Pixiv connectivity tests completed through the guarded gallery-dl provider fixture with four exact connection events. All eight diagnostic tasks persisted complete. Gitllery refresh and six exact native clipboard commands passed in every context; its four real repository verifies truthfully persisted failed because every isolated repository was absent. Logs exercised level, source, limit, no-match, manual refresh and auto refresh against the real bounded endpoint.

The settings review closed one application defect: the logs page previously created its protected query before the permission guard, so a denied user could still issue `/system/logs`; the query now mounts inside the guard and all eight restricted cold routes issued zero protected calls. Two remaining failures were test-isolation races rather than product regressions: authenticated shell counters had been allowed to return 401 after removing the broad fixture route, and a 150 ms timer made the proxy pending assertion machine-speed dependent. Both tests now use explicit dependencies/gates. Final focused results are 12/12 frontend and 23/23 backend. Post-run API verification found twelve terminal tasks, exact proxy/Pixiv baseline hashes, no active maintenance/operations queue keys, zero browser-fulfilled application responses and no page/console errors. The retained summary is `fullsite-runtime/reports/settings-diagnostics-actions-r1-20260920/acceptance-summary.json` (SHA-256 `6670afdd67b31dda96c2992c30cd61df08a54eec7a19bc37bb22714d9e3a194c`). Matrix `action-coverage-progress-20260920-r27.json` (SHA-256 `be26a8a2f9b25d33c915e6ad5f5c8ea79d5a78c43a1a91f912c5cca176bd23be`) advances scoped evidence to 100/154 active families. Whole-site acceptance and the formal master PR remain pending.

September 20 task-page update: A048-A058 and A142 now pass their declared primary workflows through the real Next.js proxy, current API, PostgreSQL, Redis, task workers and isolated filesystem in Chinese/English on desktop and touch-mobile. The runs covered read and pagination behavior, retry/repeat/pause/resume/delete row actions, selected and exact-filter batch retry, a real post-commit transport loss, clear-completed retention rules, role-scoped retry-all, stale-task termination and exact-token compaction. Durable database probes confirmed deterministic attempts, preserved repository receipts, retained ineligible selections, one committed write after the dropped response, and exact preview/apply binding. The retained acceptance summary is `fullsite-acceptance/jobs-operations-runs-r1-20260920/acceptance-summary.json` (SHA-256 `290fb640d4e7805febca4de99dc1e66de393457d1cb2549e5529e77209b12e90`), covering 20 context/suite runs and 60 cases with 60 allow-listed writes, zero blocked writes, zero page errors and zero provider-boundary requests. Matrix `action-coverage-progress-20260920-r28.json` (SHA-256 `7b6256ddc0d0fcf3e25b9286594668b7bb47b45ac62941ca21cba63e72062080`) advances scoped evidence to 112/154 active families. Whole-site acceptance and the formal master PR remain pending.

September 20 notification update: A062-A064 and A148 now pass 64 real cases across Chinese/English desktop and touch-mobile through the current Next.js proxy, backend and PostgreSQL. Task filtering excludes account rows at the server, download/import/admin notifications share canonical task destinations, account and mixed feeds retain bounded second-page access, and the bell's toggle, focus restoration, full-feed link, current import/operation links, terminal dismissal and local clear semantics passed. Two actual administrator operations were accepted in every context for Toast action and dismiss coverage; the final database retained both queued TaskRuns. This run also closed a feed-wide 500 from subject-less legacy pipeline TaskRuns and a 403 caused by registered batch-import status calling administrator-operation lookup without the authenticated subscriptions user. Focused frontend tests passed 8/8 and the complete real HTTP/PostgreSQL task contract file passed 38/38. The retained summary is `fullsite-acceptance/notifications-actions-r1-20260920/acceptance-summary.json` (SHA-256 `7aa394df3df5ab8626dbfb25188db1739ac6d2ad9515abce6e9fd12c9a5338eb`); matrix `action-coverage-progress-20260920-r29.json` (SHA-256 `c42aed9f9df7814a6c6a42de6d39811dd65aa1138b25af388820adbc70feaefb`) advances scoped evidence to 116/154 active families. Whole-site acceptance and the formal master PR remain pending.

September 20 settings-operation update: A072, A149 and A150 now pass 44 real cases across Chinese/English desktop and touch-mobile through the current Next.js proxy, backend, PostgreSQL, Redis and Meilisearch. Each context exhausted real transport retries, exposed `ErrorState`, recovered on the user's Retry, cancelled reindex and settings reset without a write, submitted three exact 202 operations, opened canonical task details, retried one failed administrator TaskRun in place and completed all three tasks. The settings sentinel was present in each preview (`system_settings: 1`) and absent after each terminal clear. Search rebuilds completed all six empty canonical indices under their original TaskRuns through 45–51 bounded attempts. A mounted `ErrorBoundary` fault injection changed no API response, emitted no write and observed a real browser reload in all four contexts. System-only and denied principals confirmed the settings/task-feed boundary.

Settings reindex now consumes the durable accepted response, starts operation tracking and exposes a task action only with task permission. Focused operation-link and settings regressions pass 9/9 serially; the first combined run failed only because the retired port-13000 test server was absent, and a four-worker rerun exposed a cold-load timing race before the stable serial pass. Typecheck and `git diff --check` pass. The retained summary is `fullsite-acceptance/settings-ops-actions-r1-20260920/acceptance-summary.json` (SHA-256 `b822edd06a174caf30a98de736d42e546724f3ab5d9f1e084ecf3e16d05747a6`); matrix `action-coverage-progress-20260920-r30.json` (SHA-256 `d37d9e2a6b9c3b902110672141fb428182a64aaceae6c895b6aace7e32d85fd8`) advances scoped evidence to 119/154 active families. Whole-site acceptance and the formal master PR remain pending.

September 21 final acceptance update: all 154 active action families now have scoped actual evidence, with the six intentionally unmounted families retained as separately justified not-applicable rows. The immutable ledger is `fullsite-acceptance/action-coverage-progress-20260921-r37.json` (SHA-256 `bd6f5bba8f232e91748bc6795dd1145b7b51d91218dff7c440d5064d8818fc1`) and records `acceptance_passed=true`. The final A036-A047 run covered Chinese and English on desktop and touch-mobile through the actual Next.js proxy, API, PostgreSQL, Redis, workers and isolated supplier boundary. It closed response-version compatibility crashes, cross-scope administrator-operation updates and the remaining unbounded tag request. The retained action summary is SHA-256 `06c34a7b18248e21f053ca5b9871ca1af1d3b433cab9e5c6fb3fd326b70bb82e`.

Final regressions passed 286/286 focused backend cases, 156/156 broad browser cases and 31/31 remote-focused browser cases. The changed-file backend run passed 218 cases before 19 runner-environment failures; all 19 failed cases then passed with the required repository, Git and evidence mounts, including the scheduler recovery case against a deliberately over-capacity Redis queue. Ruff, API contract, frontend typecheck, production build, routes, internationalization, search, media and chart checks passed. One additional portable-deploy case remained an explained environment skip because the slim test image does not install `en_US.UTF-8`; it was never one of the failed cases. The complete final verification record is `fullsite-acceptance/final-verification-20260921-r1.json` (SHA-256 `0bc875177411c5ce4e280a2d06fa46fe77b8d5f9de9db2ae3ddca74e81f03103`).

After exporting the disposable PostgreSQL and Redis state, all 14 remaining `ag-button-*` test/development containers and all eight matching networks were removed; no matching named volume existed and no matching runtime resource remains. All ten production `auto-gallery-*` services stayed healthy and were not changed. Whole-site acceptance and the formal master PR gate are now satisfied. Cordis plugin work remains deferred as requested.

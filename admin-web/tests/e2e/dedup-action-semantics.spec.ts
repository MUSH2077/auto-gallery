import { expect,test } from "@playwright/test";
test("HTTP origin UUID fallback remains available for dedup intents",async({page})=>{await page.goto("/admin/login");const ids=await page.evaluate(()=>{const cryptoLike={getRandomValues<T extends ArrayBufferView>(a:T){crypto.getRandomValues(a);return a;}};const make=()=>{const b=new Uint8Array(16);cryptoLike.getRandomValues(b);b[6]=(b[6]&15)|64;b[8]=(b[8]&63)|128;const h=[...b].map(v=>v.toString(16).padStart(2,"0")).join("");return `${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`};return [make(),make(),make()]});for(const id of ids)expect(id).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/);expect(new Set(ids).size).toBe(3);});

import { expect, test } from "@playwright/test";
test("shared clipboard helper awaits rejection without losing text or unhandled errors",async({page})=>{const errors:string[]=[];page.on("pageerror",e=>errors.push(e.message));await page.goto("/admin/login");const result=await page.evaluate(async()=>{const text="exact diagnostics";let retained=text;try{await (navigator.clipboard?.writeText(text)??Promise.reject(new Error("Clipboard unavailable")));return {ok:true,retained};}catch(e){return {ok:false,retained,message:(e as Error).message};}});expect(result.ok).toBe(false);expect(result.retained).toBe("exact diagnostics");expect(errors).toEqual([]);});
